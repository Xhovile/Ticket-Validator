Temporary diagnostic note. The authenticated Validator API returns four authorized events, but mapValidatorEvent previously assigned organizerId="". EventsView filters organizer events by organizerId === current Firebase UID, so all four authorized events were removed from the rendered list. The mapper must preserve the authenticated organizer UID when converting Validator event records.